---
title: 'Update Buyer'
api: "PUT {host}/buyer"
description: 'API endpoint to update buyer information'
---

### Headers

<ParamField header="Content-Type" type="string" required>
  application/json
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token for authorization.
</ParamField>

### Body

<ParamField body="email" type="string" required>
  The email of the buyer.
</ParamField>

<ParamField body="fullname" type="string" required>
  The full name of the buyer.
</ParamField>

<ParamField body="address" type="object" required>
  <Expandable title="address">
    <ParamField body="address" type="string" required>
      The street address.
    </ParamField>
    <ParamField body="city" type="string" required>
      The city.
    </ParamField>
    <ParamField body="state" type="string" required>
      The state.
    </ParamField>
    <ParamField body="zipcode" type="string" required>
      The postal code.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="contacts" type="object" required>
  <Expandable title="contacts">
    <ParamField body="countryCode" type="string" required>
      The country code.
    </ParamField>
    <ParamField body="mobile" type="string" required>
      The mobile number.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="buyerApp" type="object" required>
  <Expandable title="buyerApp">
    <ParamField body="subscriberID" type="string" required>
      The subscriber ID.
    </ParamField>
    <ParamField body="bapURL" type="string" required>
      The BAP URL.
    </ParamField>
    <ParamField body="country" type="string" required>
      The country.
    </ParamField>
    <ParamField body="domain" type="string" required>
      The domain.
    </ParamField>
    <ParamField body="environment" type="string" required>
      The environment.
    </ParamField>
    <ParamField body="apptype" type="string" required>
      The application type.
    </ParamField>
  </Expandable>
</ParamField>

### Response

- **200 OK**
  ```json
  {
    "message": "Buyer updated successfully",
    "buyer": {
      // Updated buyer details
    }
  }
  ```

- **400 Bad Request**
  ```json
  {
    "error": "Invalid input data"
  }
  ```

- **401 Unauthorized**
  ```json
  {
    "error": "Unauthorized access"
  }
  ```